# AI 생산관리 (PRD)

작성자: 황희원
작성일: 2026-09-14

## 1) 추진 배경 (문제 정의)

생산기술1팀은 생산효율을 검토하고 관리하는 업무를 담당하고 있다. 그러나 현재는 담당자가 생산 데이터를 직접 확인하고 정리해 분석해야 하는 방식이라, 문제를 파악하고 개선점을 찾기까지 많은 시간과 수작업이 필요하다는 불편이 있다.

## 2) 현행 방식과 한계

현재 생산 데이터 취합 자체는 수작업이 아니지만, 취합된 데이터를 정리하고 분석하는 과정은 담당자가 엑셀로 직접 처리하고 있다. 분석은 데이터 자체를 기준으로 판단하기 때문에 사람에 따라 결과가 크게 달라지지는 않지만, 정리와 분석 과정이 수작업이다 보니 시간이 오래 걸린다는 한계가 있다.

## 3) 목표와 기대효과 (성공 기준)

생산실적을 입력하면 AI가 기준 표준ST와 자동으로 비교해 차이(%)를 계산해주고, 자동화 설비의 실적도 함께 확인할 수 있도록 하는 것을 목표로 한다.
- 생산실적 입력 후 표준ST 대비 차이(%) 즉시(수 초 이내) 확인
- 자동화 설비 실적 조회: 별도 문서 없이 화면에서 바로 확인 가능

## 4) 사용자와 이용 흐름

주 사용자는 생산기술1팀 담당자(본인)이다.

이용 흐름: 생산실적 입력 → ① 공정별 표준ST와 비교해 차이(%) 확인 / ② 자동화 설비 실적을 바탕으로 ROI 계산
- 기준 데이터(공정별 표준ST)는 이후에도 자유롭게 수정할 수 있어야 한다.

## 5) 주요 기능 (must / nice로 구분)

**Must 1. 표준ST 대비 생산실적 비교**
- 입력값: 공정 선택(기본은 assy 단위이며, 작업이 assy에서 끝나지 않은 경우 하위 1단계 → 하위 2단계까지 선택 가능한 계층 구조. 이 계층 구조는 이후에도 수정 가능해야 한다), 시작시간, 끝시간, 생산수량.
- 근무시간(08:30~17:30) 중 점심시간(11:30~12:30)과 휴식시간(10:00~10:10, 14:00~14:10, 15:40~15:50)은 생산시간에서 자동으로 제외하고 계산하며, 결과는 분(分) 단위로 저장한다.
- 표준ST는 웹 화면에서 사용자가 직접 입력하며, 단위는 "1개 생산에 필요한 시간(초)"이다.
- 공정 목록은 웹 화면에서 추가·수정·삭제할 수 있어야 한다.
- 과거에 입력한 생산실적은 목록으로 다시 조회할 수 있어야 한다.
- 기간(일/주/월 등) 단위로 입력된 생산실적의 평균 시간을 계산해, 표준ST와 비교했을 때 얼마나 일치하는지 확인할 수 있다.
- 표준ST 대비 실적 비교 결과는 야마즈미 차트(공정별 소요시간을 누적 막대로 쌓아 보여주고, 표준ST(목표선)를 함께 표시하는 차트) 형태로 화면에 보여준다.
- AI가 지킬 규칙: 표준ST 값은 사용자가 언제든 수정할 수 있어야 하며, 수정 시 이후 계산에는 반드시 최신 값을 사용한다. 표준ST(초)와 생산시간(분)의 단위가 다르므로, 비교 시 반드시 단위를 맞춰 계산한다. 표준ST가 아직 등록되지 않은 공정은 실적만 우선 저장해두고, 표준ST가 등록되면 그때부터 자동으로 비교 계산한다. 생산수량·시간 값은 음수를 입력할 수 없다.

**Must 1-1. 생산팀별 데이터 분리**
- 파트(1단계)마다 소속 생산팀(현재 생산1팀/생산2팀/생산3팀이며, 화면에서 + 버튼으로 팀을 자유롭게 추가할 수 있다)을 지정한다. 자동화 설비도 팀별로 따로 관리한다.
- 생산실적입력·공정 설정·자동화 설비 설정·대시보드 화면은 모두 팀 탭으로 전환해서 팀별 데이터를 따로 보고 입력한다.
- 지원(생산지원)·연차대응으로 실적을 입력할 때는 같은 팀 안의 다른 파트 공정도 고를 수 있다(파트 간 교차 지원). 다만 다른 팀의 공정까지 넘어가지는 않는다.

**Must 2. 자동화 설비 ROI(투자비 회수) 계산**
- 자동화 설비는 여러 대이며, 설비별로 각각 투자비용 대비 "지금까지 얼마나 회수했는지(본전 회수액)"를 관리한다.
- 생산실적 입력 시 어떤 자동화 설비를 사용했는지 선택할 수 있어야 하며, 해당 실적은 그 설비의 ROI 계산에 반영된다.
- ROI 계산에 쓰이는 값과 계산식 자체(예: 설비 도입비용, 절감액 산정 기준 단가 등)는 코드 수정 없이 웹 화면에서 사용자가 직접 설정·수정할 수 있다.
- 시작 시점에 설비별로 기존 누적 회수액을 한 번 입력해둘 수 있으며(초기값 설정), 이후 새로 입력하는 실적은 그 값 위에 누적 합산된다.
- 과거에 입력한 ROI 관련 실적도 목록으로 다시 조회할 수 있어야 한다.
- AI가 지킬 규칙: 계산에 사용한 입력값과 계산식을 함께 보여줘서 검증 가능하게 한다. 계산식을 수정하면, 과거에 입력해둔 실적을 포함해 화면에 보여줄 때는 항상 "지금" 계산식·값 기준으로 다시 계산한다(저장된 옛 계산값을 그대로 보여주지 않는다). 아직 실적 입력이 다 끝나지 않은 최근 기간은 필터로 제외하고 볼 수 있어야 한다. 누적 합계는 설비별로 새로고침·재접속해도 유지되어야 한다. 금액·수량 값은 음수를 입력할 수 없다.

## 6) 범위와 비범위

**이번에 만들 것 (범위)**
- 생산실적 입력 화면 (공정 선택, 시작~끝시간, 생산수량 입력 + 점심·휴식시간 자동 차감)
- 공정 목록 관리 (assy 단위 + 하위 2단계까지의 계층 구조, 추가·수정·삭제 가능)
- 공정별 표준ST 등록/수정 기능
- 표준ST 대비 실적 차이(%) 계산 및 표시
- 자동화 설비별 ROI(투자비 회수) 계산 및 표시 (설비별 계산식 설정, 시작 시점 누적 초기값 설정 포함)
- 과거에 입력한 생산실적·ROI 이력 조회 화면
- 누적 값을 새로고침·재접속 후에도 유지하기 위한 데이터 저장. Vercel 배포 환경은 파일 시스템에 쓸 수 없어, Supabase Postgres의 단순한 4개 테이블(teams/processes/equipments/records)로 최소한만 구성했다(복잡한 조인·트랜잭션 없이 화면과 1:1로 대응)
- 데이터 내보내기(다운로드) 버튼 — 필요할 때 백업 파일을 직접 받을 수 있게 함
- 편집(입력/수정) 시 비밀번호 입력 (조회는 비밀번호 없이 누구나 가능, 편집만 비밀번호로 보호. 이후 이메일 로그인 도입 시 대체 예정)

**이번에 절대 만들지 않을 것 (비범위)**
- 회원가입/이메일 로그인, 여러 사용자 권한 관리 (이번엔 편집 시 비밀번호 하나로 최소한만 보호하고, 이메일 로그인은 다음 단계에서 추가 예정)
- 복잡한 데이터베이스 구축·다중 테이블 연동 (Supabase의 단순 4개 테이블 수준을 넘는 스키마·조인은 만들지 않는다)
- 생산조건·근무시간 기반 타임테이블 시각화 기능 (다음 단계에서 별도 진행)
- 야마즈미 차트를 작업자/설비별로 세분화하는 기능 (지금은 공정 기준으로만 표시, 다음 단계에서 추가 가능)
- 이상 징후 자동 알림/예측 기능
- 모바일 앱 (웹 화면만)

## 7) 보안·개인정보 검토

- 공개등급: 팀 내부 공유 자료 (배포된 링크를 아는 사람은 로그인 없이 조회 가능 — 생산기술1팀 내부 공유 목적). 편집(입력/수정)은 비밀번호 입력자만 가능하며, 이메일 로그인 방식은 다음 단계에서 추가 예정
  - (진행 상황) Microsoft 365(Azure AD) 계정으로 로그인하는 기능은 구현을 마쳤으나, 회사 Azure 관리자 동의가 아직 나지 않아 잠시 보류 중이다. 동의를 받으면 이 방식으로 전환할 예정이다 (관련 코드: `trash-can/proxy.ts`, `src/app/login`, `src/lib/supabase`).
- 개인정보 여부: 개인정보는 다루지 않음 (생산실적·표준ST·설비 데이터 등 업무 데이터만 사용)
- API 키 관리: AI 기능에 필요한 API 키는 코드에 직접 넣지 않고 환경변수(.env)로 관리하며, git에 커밋하지 않고 Vercel 환경변수로 별도 등록한다.

## 8) 기술 스택

- Next.js: 화면과 서버 쪽 코드(API)를 한 프로젝트에서 처리
- 고정 이유: STEP 2에서 my-app에 이미 설치했고, Part 5에서 Vercel로 배포할 예정이라 그대로 이어지게 하기 위함
